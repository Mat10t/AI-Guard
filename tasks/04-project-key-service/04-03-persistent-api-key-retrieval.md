# Заголовок
Возврат полного API-ключа проекта для активного ключа

## Контекст
Текущий UX неудобен: после обновления страницы доступен только `prefix`, из-за чего пользователь не может скопировать рабочий `api_key`.

## Что нужно сделать
- Расширить схему `api_keys` полем хранения полного `api_key` для MVP.
- Обновить `POST /projects/{id}/key`, чтобы сохранять полный ключ.
- Обновить `GET /projects/{id}/key`, чтобы возвращать `api_key` для активного ключа.
- Сохранить совместимость текущих ответов (`id`, `status`, `prefix`).

## Связанный сервис / модуль
`project-key-service`, `db`.

## Зависимости
- `04-02-project-api-key.md`

## Приоритет
P1

## MVP
да

## Критерии готовности
- После вызова `GET /projects/{id}/key` для активного ключа клиент получает `api_key`.
- Проверка ключа в gateway продолжает работать по `key_hash`.
- Существующие сценарии `issue/revoke/check` не регрессируют.
