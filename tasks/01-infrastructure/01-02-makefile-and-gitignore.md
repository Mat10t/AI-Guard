# Заголовок
Makefile и .gitignore для локальной разработки

## Контекст
Нужно стандартизировать команды сборки, запуска и тестирования для команды.

## Что нужно сделать
- Добавить root `Makefile` с целями `test`, `build`, `up`, `down`, `rebuild`.
- Добавить `.gitignore` под Go/Node/docker-артефакты.
- Добавить короткую справку команд в `README.md`.

## Связанный сервис / модуль
Infrastructure / developer experience.

## Зависимости
- `01-01-local-stack-compose.md`.

## Приоритет
P0

## MVP
да

## Критерии готовности
- Все минимальные команды запускаются из корня проекта.
- Локальные артефакты не попадают в git.
- Команды документированы и воспроизводимы.
