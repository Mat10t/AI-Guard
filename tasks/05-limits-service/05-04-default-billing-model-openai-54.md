# Заголовок
Лимиты: дефолтная billing-модель `gpt-5.4-mini`

## Контекст
После перехода каталога на линейку 5.4 дефолтная модель лимитов должна совпадать с актуальным каталогом и UI.

## Что нужно сделать
- Заменить дефолт `billing_model` в limits-service на `gpt-5.4-mini`.
- Сохранить текущую логику budget/token sync без изменения контрактов.
- Обеспечить совместимость с payload без явного `billing_model` (сервер ставит новый дефолт).

## Связанный сервис / модуль
`limits-service`.

## Зависимости
- `05-03-project-budget-sync.md`
- `07-03-openai-54-model-catalog.md`

## Приоритет
P0

## MVP
да

## Критерии готовности
- При пустом `billing_model` сохраняется `gpt-5.4-mini`.
- Budget-sync продолжает работать по формуле 50/50.
- Ошибка для `mock-fast` при `sync_source=budget` сохраняется.
